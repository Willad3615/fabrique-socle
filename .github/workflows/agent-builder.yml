name: Agent Builder

# Interactif : répond à « @claude » dans une issue ou une pull request.
# C'est l'agent qui écrit le code. Il pousse sur une branche et ouvre une PR ;
# il ne peut ni pousser sur main ni fusionner — la règle de branche l'en empêche.

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, labeled]

jobs:
  builder:
    if: |
      contains(github.event.comment.body, '@claude') ||
      (github.event_name == 'issues' && contains(github.event.issue.labels.*.name, 'fabrique:construire'))
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          claude_args: |
            --max-turns 30
